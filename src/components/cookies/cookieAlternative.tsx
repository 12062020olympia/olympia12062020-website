import React, { useIntl } from 'gatsby-plugin-intl';
import { FC } from 'react';

interface Props {
  alternativeLink?: string;
}

const CookieAlternative: FC<Props> = () => {
  const intl = useIntl();

  return <></>;
};

export default CookieAlternative;
